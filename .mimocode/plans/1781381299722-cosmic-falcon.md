# Plan: Sidebar Navigation — Add All Missing Pages

## Goal
Add all 10 existing-but-unlinked pages to the sidebar navigation, organized into logical sections with proper icons and translations.

## Files to Modify

| File | Change |
|------|--------|
| `apps/web/components/icons/NavIcons.tsx` | Add 7 new stroke icons (training, benchmark, tokenizer, export, labs, agents, login) |
| `apps/web/components/Sidebar.tsx` | Expand `navItems` from 6 to 16 items, add "System" section, two-group layout |
| `apps/web/hooks/useLocale.tsx` | Add 7 new `nav.*` translation keys × 5 locales (en/es/fr/de/zh) |

## Step 1: New Icons (NavIcons.tsx)

Add 7 minimal stroke icons matching the existing 24×24 viewBox pattern:

| Icon | For Page | Design |
|------|----------|--------|
| `IconTraining` | `/training` | Bar chart (3 vertical bars rising) |
| `IconBenchmark` | `/benchmark` | Stopwatch/timer circle |
| `IconTokenizer` | `/tokenizer` | Letter "A" with split line (tokenize) |
| `IconExport` | `/export` | Arrow-up-right from box (export out) |
| `IconLabs` | `/labs` | Flask/beaker |
| `IconAgents` | `/agents` | Two people silhouette |
| `IconLogin` | `/login` | Arrow-right-into-door |

Existing icons reused: `IconCompare` (/compare), `IconActivity` (/monitoring), `IconAlert` (/errors), `IconSearch` (/multimodal — or create `IconMultimodal`).

## Step 2: Locale Translations (useLocale.tsx)

Add to all 5 locales (en/es/fr/de/zh):

| Key | en | es | fr | de | zh |
|-----|----|----|----|----|-----|
| `nav.training` | Training | Entrenamiento | Entraînement | Training | 训练 |
| `nav.benchmark` | Benchmark | Benchmark | Benchmark | Benchmark | 基准测试 |
| `nav.tokenizer` | Tokenizer | Tokenizador | Tokeniseur | Tokenizer | 分词器 |
| `nav.export` | Export | Exportar | Exporter | Exportieren | 导出 |
| `nav.labs` | Labs | Laboratorio | Laboratoire | Labor | 实验室 |
| `nav.agents` | Agents | Agentes | Agents | Agenten | 代理 |
| `nav.login` | Sign In | Iniciar sesión | Connexion | Anmelden | 登录 |

## Step 3: Sidebar Restructuring (Sidebar.tsx)

### Current: 6 items in single flat list
### New: 16 items in 2 sections

**Section 1 — Workspace** (8 items, user-facing):
1. `/chat` — IconChat
2. `/conversations` — IconMenu
3. `/training` — IconTraining
4. `/models` — IconModels
5. `/datasets` — IconActivity
6. `/knowledge` — IconSearch
7. `/compare` — IconCompare
8. `/settings` — IconSettings

**Section 2 — System** (8 items, admin/ops):
1. `/monitoring` — IconActivity
2. `/errors` — IconAlert
3. `/benchmark` — IconBenchmark
4. `/tokenizer` — IconTokenizer
5. `/export` — IconExport
6. `/labs` — IconLabs
7. `/agents` — IconAgents
8. `/login` — IconLogin

The `sidebar.system` translation key already exists in the locale file.

## Step 4: Implementation Details

### Sidebar.tsx Changes
- Split `navItems` into `workspaceItems` and `systemItems` arrays
- Render two `<ul>` groups with existing `sidebar.workspace` and `sidebar.system` section headers
- Reuse existing `navLinkClass` for both sections
- Add thin `<Divider>` or `<hr>` between sections (border-t)

### NavIcons.tsx Pattern
Each icon follows existing pattern:
```tsx
export function IconXxx({ className = navSize }: IconProps) {
  return (
    <svg className={`${base} ${className}`} viewBox="0 0 24 24" fill="none" aria-hidden>
      <path d="..." stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  )
}
```

## Verification

1. `npx tsc --noEmit` — TypeScript compiles with 0 errors
2. Visual check: sidebar renders all 16 items in 2 sections
3. Active state: clicking each nav item highlights correctly
4. Mobile drawer: all items accessible in drawer variant
5. Translations: switching locale updates all nav labels

## Risk Notes
- 16 items may feel crowded on short viewports — the sidebar already has `overflow-y-auto` on the nav section, so scrolling handles this
- `/login` in sidebar is somewhat unusual — could be placed in account dropdown instead. Will include it in sidebar for discoverability but could move to dropdown if preferred.
