[pytest]
testpaths = tests
addopts = -n auto --dist loadgroup -q
asyncio_mode = auto
markers =
    slow: tests that take more than a few seconds
    integration: tests that require external services or live API
