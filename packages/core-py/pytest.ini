[pytest]
testpaths = tests
addopts = -n auto --dist loadgroup -q -m "not slow"
asyncio_mode = auto
markers =
    slow: tests that take more than a few seconds (skip with -m "not slow")
    integration: tests that require external services or live API
