# SloughGPT Requirements
# ====================
# Install with: pip install -r requirements.txt
#
# RECOMMENDED: Use conda for PyTorch on macOS Intel
#   conda create -n sloughgpt python=3.11
#   conda activate sloughgpt
#   conda install pytorch torchvision torchaudio -c pytorch
#   pip install -r requirements-no-torch.txt

# Core ML (PyTorch is optional — install separately for HuggingFace or legacy trainers)
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Data Processing
pillow>=10.0.0
requests>=2.28.0

# Web/API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
starlette>=0.27.0
httpx>=0.25.0
pydantic>=2.13.5
pydantic-settings>=2.14.2

# Async
aiofiles>=25.1.0
aiohttp>=3.8.0

# Authentication
PyJWT>=2.13.0
python-multipart>=0.0.32

# CLI/UI
click>=8.1.0
prompt-toolkit>=3.0.53

# Storage/IO
safetensors>=0.4.0

# System monitoring
psutil>=7.2.2

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
dataclasses-json>=0.6.7

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.7.0

# Optional: ONNX export
onnx>=1.22.0

# Optional: GGUF support (requires llama.cpp)
# llama-cpp-python>=0.2.0

# Optional: MLflow for experiment tracking
# mlflow>=2.8.0

# Optional: Weights & Biases
# wandb>=0.16.0
